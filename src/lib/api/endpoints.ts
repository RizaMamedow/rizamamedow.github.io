const ApiEndpoints = {
    certificates: {
        root: "api/certificates",
    },

    skills: {
        root: "api/skills",
    },

    contacts: {
        root: "api/contacts",
    },
};

export default ApiEndpoints;
